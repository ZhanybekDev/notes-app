# Plan Review — zustand-migration (проход 10)

**Timestamp:** 2026-07-30 14:01:46
**Verdict:** `APPROVE_WITH_CHANGES`
**Complexity:** MEDIUM
**Plan source:** `.claude/plans/current-plan.md` (517 строк, 42 acceptance criteria)
**Предыдущие отчёты:** `13-26-06`, `iter2`–`iter9 13-57-09`
**Probed assumptions:** 2 новых (42 суммарно)
**Failed assumptions:** 2
**Applied fixes:** 4
**Critical findings:** 0

В отчёте iter9 я написал: «десятый проход не имеет предмета — и это уже не прогноз, а проверяемое
утверждение». Утверждение оказалось ложным, **третий раз подряд**. Угол, которого не было ни в одном
из девяти проходов: план перечисляет места в `docs/architecture.md`, которые ломает, — но полноту
этих перечислений никто не проверял `grep`'ом. Проверил. Одно из двух оказалось неполным.

---

## Assumption Register (проход 10)

| # | Assumption | Проверка | Статус |
|---|---|---|---|
| 18 (пересмотр) | `auth.js` упомянут в `docs/architecture.md` трижды | `grep -n "auth\.js\|AUTH\["` | ✅ ровно три: `:94`, `:102`, `:212` |
| 40 | Фаза 4 перечисляет все места, ломаемые снятием Context | `grep -n "i18n\.jsx\|LangProvider\|React Context"` | ❌ **три из пяти** |

---

## Findings by Dimension

### D2. Cross-Component Pipeline / Hard Rules

**M24 — фаза 4 называла три места из пяти, и по ней документ остался бы с двумя ложными
утверждениями.** План предписывал: «снять описание `i18n.jsx` как React Context provider (раздел
Frontend packages и дерево Frontend layout), убрать `LangProvider` из описания `main.jsx`». Полный
`grep` даёт шесть упоминаний, из которых пять становятся ложными после фазы:

| строка | текст | статус после фазы 4 | был ли в плане |
|---|---|---|---|
| `:66` | «matching `i18n.jsx`» (сопоставление языка) | остаётся верным | — |
| `:92` | `COMP --> I18N[i18n.jsx]` | остаётся верным (компоненты по-прежнему берут `useLang`) | — |
| `:95` | `MAIN --> I18N` | **ложно** — `main.jsx` больше не импортирует `i18n.jsx`, оборачивать нечем | ❌ пропущено |
| `:99` | «`main.jsx` … wraps the tree in `LangProvider`» | **ложно** | ✅ |
| `:104` | «`i18n.jsx` — React Context provider» | **ложно** | ✅ |
| `:209` | дерево layout: «`main.jsx` bootstraps LangProvider + Router + App» | **ложно** | ❌ пропущено |
| `:214` | дерево layout: «`i18n.jsx` React Context, EN + RU» | **ложно** | ✅ |

Пропущены ребро mermaid-графа и строка дерева про `main.jsx`. Оба — не косметика: в этом проекте
`docs/architecture.md` служит критерием Hard Rules (своего `CLAUDE.md` нет), и дрейф документа
фиксировался как нарушение на проходах 1 и 3. Фаза, которая сама себе ставит задачу «привести
документ в соответствие», и при этом называет 60% мест, оставляет после себя ровно ту проблему,
которую бралась закрыть.

**Applied:** пункт переписан с полным перечислением пяти мест по номерам строк, с явным указанием
двух упоминаний, которые остаются верными и **не** правятся (иначе следующий исполнитель начнёт
править и их), и с врезкой о том, почему перечисление поимённо здесь обязательно. Добавлен критерий:
`grep -n "LangProvider" docs/architecture.md` пуст.

**Проверка симметричного пункта фазы 3 — чисто.** `auth.js` упомянут ровно трижды (`:94` ребро графа,
`:102` список модулей, `:212` дерево layout), и все три названы в плане. Здесь перечисление было
полным.

### D1, D3–D12

Чисто. Заодно подтверждены остальные ссылки плана на строки документа: `:29` — строка таблицы
сервисов «React 18, Vite, React Router» ✅, `:42` — «the JWT in `localStorage` is its only persistent
local state» ✅. Правки `docs/architecture.md`, сделанные сегодня в коммите `8ed11d8`, лежат ниже
строки 253 и цитируемых номеров не сдвинули.

---

## Hard Rules Violations

Нет — после применения M24. До правки фаза 4 нарушала бы правило соответствия документа коду, но это
и есть содержание M24, дублировать в отдельную секцию не буду.

---

## Applied Fixes (summary)

- **[M24]** фаза 4: полное перечисление пяти ломаемых мест в `docs/architecture.md` (`:95`, `:99`,
  `:104`, `:209`, `:214`) плюс явный список двух, которые не правятся
- **[M24]** критерий «`grep -n "LangProvider" docs/architecture.md` пуст»
- Пересмотр строки 18 и новая строка 40 в Assumption Register плана

---

## Unresolved

Нет.

---

## Динамика десяти проходов

| Проход | Critical | Missing | Угол |
|---|---|---|---|
| 1–4 | 4+2HR / 3 / 2 / 1 | 5 / 3 / 2 / 2 | содержание фаз |
| 5 | 1 | 3 | эстимация, симметрия |
| 6 | 1 | 2 | инвентарь тестов |
| 7 | 0 | 3 | сверка артефактов ↔ критериев |
| 8 | 0 | 1 | числа: `useState` |
| 9 | 0 | 2 | числа: тесты и моки |
| 10 | **0** | **1** | полнота перечислений внутри плана |

Четыре прохода подряд без Critical. Три прохода подряд я объявлял серию исчерпанной и три раза
ошибался — причём каждый раз находка приходила из угла, который в момент прогноза не был даже
сформулирован. Честный вывод после десяти проходов: **предсказать исчерпание нельзя, потому что
неизвестен список возможных углов.** Что можно сказать точно — цена находок: последние три прохода
дали неверное число, неверную базовую линию и неполное перечисление. Ни одна из них не меняла
архитектуру, поведение или объём работы; все три меняли точность инструкций, по которым работает
ship-it.

Прагматический критерий остановки, а не эпистемологический: план описывает 4–5.5 часа работы и
занимает 517 строк с 42 критериями и 42 проверенными assumptions. Инструкции точны настолько,
насколько это проверяемо чтением. Оставшиеся риски — поведенческие, и первый же прогон фазы 1
проверит их лучше, чем одиннадцатый проход ревью.

---

## Verdict Rationale

Ноль Critical, один Missing — применён. Фаза 4 обещала привести документ в соответствие с кодом и
называла три места из пяти; по такому плану документ остался бы с двумя ложными утверждениями, а
именно этот документ в проекте служит критерием Hard Rules. Полнота перечислений — тот угол, который
девять предыдущих проходов не проверяли ни разу.

---

## Next Action

`/ship-it` с фазы 1.

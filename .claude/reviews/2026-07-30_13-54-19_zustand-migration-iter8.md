# Plan Review — zustand-migration (проход 8)

**Timestamp:** 2026-07-30 13:54:19
**Verdict:** `APPROVE_WITH_CHANGES`
**Complexity:** MEDIUM
**Plan source:** `.claude/plans/current-plan.md` (500 строк, 41 acceptance criterion)
**Предыдущие отчёты:** `13-26-06`, `iter2`–`iter7 13-50-30`
**Probed assumptions:** 2 новых (38 суммарно)
**Failed assumptions:** 1
**Applied fixes:** 4
**Critical findings:** 0

В отчёте iter7 я написал, что непроверенных углов не осталось. Это было неверно: два оставались, и
оба проверялись впервые именно здесь — (1) фактическая проверка API, на который опираются
тест-стратегии, по официальной документации, и (2) сверка **чисел** плана с кодом. Второй дал
находку.

---

## Assumption Register (проход 8)

| # | Assumption | Проверка | Статус |
|---|---|---|---|
| 37 | Числа в плане (сколько `useState` где) верны | `grep -c "= useState"` против `grep -c "useState"` | ❌ все три занижены на единицу |
| 38 | `store.persist.rehydrate()` — реальный API | `docs/reference/middlewares/persist.md`, строки 905 и 978 | ✅ существует |

---

## Findings by Dimension

### D1. Domain Model Audit

**M21 — три числа в плане неверны, и одно из них стоит в implementation outline.** Проверка
`grep -c "= useState"`:

```
pages/Notes.jsx:    14
pages/Settings.jsx: 11
pages/Calendar.jsx:  6
```

План утверждал: «`Notes.jsx` держит 13 `useState`, `Settings.jsx` — 12» (Goal и Context) и
«удаляются 11 `useState`» (шаг 4 фазы 1). Все три мимо. Происхождение ошибки стоит зафиксировать:
первый скан считал `grep -c "useState"`, а эта строка есть ещё и в `import { useEffect, useMemo,
useState } from 'react'` — то есть счётчик включал импорт. Дальше числа разошлись сами: в `Notes.jsx`
получилось на единицу меньше, в `Settings.jsx` — на единицу больше.

Формулировка «удаляются 11» опаснее остальных, потому что по ней работает ship-it: из четырнадцати
хуков в фазе 1 уезжают ровно двенадцать (`items` … `error`, строки 12–23), а `reminderPrefs` и
`reminderPrefsFailed` (25–26) уходят в фазе 2 вместе с настройками. Указание «одиннадцать» — это
инструкция оставить один лишний и не заметить, потому что план так и сказал.

**Applied:** Goal — «четырнадцати»; Context — «14 `useState`, `Settings.jsx` — 11» со ссылкой на
способ подсчёта и строки; шаг 4 фазы 1 — «уезжают 12 (строки 12–23)» с явной оговоркой, что
оставшиеся два уходят в фазе 2, а не забыты здесь.

### D10. Input Quality Audit

Проверка `persist.rehydrate()` — единственная фактическая опора плана, ранее принятая на слово.
Подтверждена по официальной документации persist (примеры `positionStore.persist.rehydrate()`).
Тест-стратегии фаз 3 и 4 стоят на существующем API. Внесена в реестр как ✅, чтобы следующий проход
не перепроверял.

### D2–D9, D11, D12

Чисто. Отдельно перепроверены три критерия, добавленных на проходе 7: ни один не противоречит
assumption 12 (лок обновляем, но образ ставит по `package.json`), тест-стратегии фазы 1 и артефактам
фазы 3.

---

## Hard Rules Violations

Нет.

---

## Applied Fixes (summary)

- **[M21]** три числа приведены к коду: 14 / 11 / 12, с указанием строк и способа подсчёта
- **[M21]** в шаге 4 фазы 1 явно сказано, что два оставшихся хука уезжают в фазе 2
- Плюс строки 37 и 38 в Assumption Register плана (вторая — подтверждение API, а не дефект)

---

## Unresolved

Нет.

---

## Динамика восьми проходов

| Проход | Critical | Missing | Порождено правками ревью | Угол |
|---|---|---|---|---|
| 1 | 4 + 2 HR | 5 | — | содержание фаз |
| 2 | 3 | 3 | 1 | содержание фаз |
| 3 | 2 | 2 | 1 | содержание фаз |
| 4 | 1 | 2 | 1 | содержание фаз |
| 5 | 1 | 3 | 1 | эстимация, симметрия |
| 6 | 1 | 2 | 0 | инвентарь тестов |
| 7 | 0 | 3 | 0 | сверка артефактов ↔ критериев |
| 8 | **0** | **1** | 1 (собственный просчёт первого скана) | числа и факты об API |

Два прохода подряд без Critical, и находка восьмого — арифметическая, хотя и попавшая в
инструкцию для исполнителя. Дважды подряд я объявлял серию исчерпанной и дважды ошибался, поэтому
на третий раз формулировка другая: **непроверенные углы, скорее всего, ещё есть, но их цена уже выше
цены ошибки, которую они предотвращают.** План 500 строк на 4–5.5 часа работы; следующая находка
масштаба «одно неверное число» стоит дешевле, чем проход, который её ищет.

---

## Verdict Rationale

Ноль Critical, один Missing — применён. По правилу скилла это `APPROVE_WITH_CHANGES`: `APPROVE`
требует нуля findings всех категорий. Существенно другое: за восемь проходов проверено 38
assumptions, шестнадцать провалились, и каждая провалившаяся отражена в плане как обоснование
конкретного решения, а не как оговорка.

---

## Next Action

`/ship-it` с фазы 1.
